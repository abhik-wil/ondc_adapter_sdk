{
  "devDependencies": {
    "@types/node": "20.11.5",
    "gts": "^5.3.0",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.4.5"
  },
  "scripts": {
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm.cmd run compile",
    "pretest": "npm.cmd run compile",
    "posttest": "npm.cmd run lint",
    "init": "ts-node -r tsconfig-paths/register ./src/index.ts"
  },
  "dependencies": {
    "axios": "^1.7.2",
    "yaml": "^2.4.2"
  },
  "engines": {
    "node": ">=18"
  }
}
